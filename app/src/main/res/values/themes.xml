<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash theme: shown only while the app is launching, then hands off to the
         normal (Compose) theme via postSplashScreenTheme. -->
    <style name="Theme.Redline.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@android:style/Theme.Black.NoTitleBar</item>
    </style>
</resources>
